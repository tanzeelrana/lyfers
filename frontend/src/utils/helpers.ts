export function sortProperty(
    properties: any[],
    sortOrderNumber: number,
    upsortOrderNumber: number,
    index: number
  ): any {
    // Your function logic here
    // Make sure to replace `any` with the appropriate types for your input and output data.
  }
  